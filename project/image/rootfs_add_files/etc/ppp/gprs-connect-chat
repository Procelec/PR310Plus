# set timeout
TIMEOUT     15

# exit the script when received “DELAYED”, “BUSY”, “ERROR”, “NO DIALTONE”, or “NO CARRIER”
ABORT       "DELAYED"                
ABORT       "BUSY"
ABORT       "ERROR"
ABORT       "NO DIALTONE"            
ABORT       "NO CARRIER"

# send AT expect nothing, send AT directly
""      AT

# look up the AT command manual for the meanings below
OK      ATS0=0
OK      ATE0V1

# setup APN
OK      AT+CGDCONT=1,"IP","\T"

# dial-up to turn on GPRS service, the number depends on mobile provider
OK      ATD*99***1#

# expect to receive CONNECT
CONNECT     ""